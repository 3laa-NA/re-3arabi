.gradle/
.vscode/
.idea/

# Per-module Gradle build output (intermediates, not the published plugins).
# The root-level /build/ is intentionally tracked - it holds the published .cs3 files.
/*/build/
/build/reports/
